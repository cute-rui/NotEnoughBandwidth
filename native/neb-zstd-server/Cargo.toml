[package]
name = "neb-zstd-server"
description = "Remote zstd offload server for NotEnoughBandwidth (UCX/RoCE)"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "neb-zstd-server"
path = "src/main.rs"

[dependencies]
neb-offload-core = { path = "../neb-offload-core" }
ucx-ffi = { path = "../ucx-ffi" }
