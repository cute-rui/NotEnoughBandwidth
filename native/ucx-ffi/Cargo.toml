[package]
name = "ucx-ffi"
description = "Minimal bindgen bindings and a thin safe wrapper over UCX UCP (Linux only)"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.69"
pkg-config = "0.3"
